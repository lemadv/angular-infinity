import { InfinityInputDirective } from './input.directive';

describe('InfinityInputDirective', () => {
  it('should create an instance', () => {
    const directive = new InfinityInputDirective();
    expect(directive).toBeTruthy();
  });
});
